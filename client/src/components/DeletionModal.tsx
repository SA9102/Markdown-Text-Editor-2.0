import { Modal, Text } from "@mantine/core";

const DeletionModal = ({ opened, onClose }) => {
  <Modal opened={opened} onClose={onClose}>
    <Text>Hello</Text>
  </Modal>;
};

export default DeletionModal;
